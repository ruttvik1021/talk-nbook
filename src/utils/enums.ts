export enum RoleEnums {
  SUPERADMIN = 'SUPERADMIN',
  USER = 'USER',
}

export enum BookingStatus {
  BOOKED = 'Booked',
  VACANT = 'Vacant',
  LAPSED = 'Lapsed',
  CANCELLED = 'CANCELLED',
  EXPIRED = 'EXPIRED',
}

export enum PaymentStatus {
  SUCCESS = 'Success',
  FAIL = 'Fail',
}
